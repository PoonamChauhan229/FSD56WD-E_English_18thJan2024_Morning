import { createContext } from"react"

// createcontext


const namingContext=createContext({
    ctxtchildName:"UseContext Child Name is available to Use!!!",
    ctxchildnickname:"used CreateContext"
})

export default namingContext;